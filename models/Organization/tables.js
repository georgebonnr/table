exports = {
  min: Number,
  max: Number,
  name: String,
  canCombine: Boolean,
  location: {
    left: Number,
    right: Number,
    top: Number,
    bottom: Number
  },
  noReservations: Boolean
};
